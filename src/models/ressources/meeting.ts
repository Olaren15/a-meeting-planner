export interface MeetingRessource {
    id: string;
    encrypted_data: string;
}

export interface CreateMeetingRessource {
    encrypted_data: string;
}

export interface UpdateMeetingRessource {
    encrypted_data: string;
}
